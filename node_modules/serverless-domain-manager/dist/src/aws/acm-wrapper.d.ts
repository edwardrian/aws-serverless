import { ACMClient } from "@aws-sdk/client-acm";
import DomainConfig = require("../models/domain-config");
declare class ACMWrapper {
    acm: ACMClient;
    constructor(credentials: any, endpointType: string);
    getCertArn(domain: DomainConfig): Promise<string>;
    private getCertArnByCertName;
    private static getCertArnByDomainName;
}
export = ACMWrapper;
