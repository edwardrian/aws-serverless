"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
const globals_1 = __importDefault(require("../globals"));
const logging_1 = __importDefault(require("../logging"));
class APIGatewayBase {
    /**
     * Gets the domainNameId for a private custom domain.
     * Returns undefined for non-private domains or if not found.
     */
    getDomainNameIdForPrivateDomain(domain) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            if (domain.endpointType !== globals_1.default.endpointTypes.private) {
                return undefined;
            }
            if ((_a = domain.domainInfo) === null || _a === void 0 ? void 0 : _a.domainNameId) {
                return domain.domainInfo.domainNameId;
            }
            return this.fetchPrivateDomainNameId(domain);
        });
    }
    /**
     * Resolves domainNameId for private domains used in getCustomDomain.
     * Returns the domainNameId, or undefined if not private.
     * Returns null to signal the caller should return early (not found).
     */
    resolvePrivateDomainNameId(domain, silent) {
        return __awaiter(this, void 0, void 0, function* () {
            if (domain.endpointType !== globals_1.default.endpointTypes.private) {
                return undefined;
            }
            const domainNameId = yield this.getDomainNameIdForPrivateDomain(domain);
            if (!domainNameId) {
                if (!silent) {
                    throw new Error(`${this.versionPrefix} - Unable to find domainNameId for private domain '${domain.givenDomainName}'`);
                }
                logging_1.default.logWarning(`${this.versionPrefix} - '${domain.givenDomainName}' does not exist or is not a private domain.`);
                return null;
            }
            return domainNameId;
        });
    }
}
module.exports = APIGatewayBase;
